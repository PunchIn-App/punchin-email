{
  "name": "punchin-email",
  "version": "1.1.1",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "vitest": "^4.1.0",
    "wrangler": "^4.97.0"
  }
}
