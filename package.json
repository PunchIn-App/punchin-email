{
  "name": "punchin-email",
  "version": "1.6.1",
  "private": true,
  "license": "BUSL-1.1",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "vitest": "^4.1.8",
    "wrangler": "^4.98.0"
  }
}
