{
  "name": "punchin-email",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "vitest": "^2.1.0",
    "wrangler": "^3.0.0"
  }
}
