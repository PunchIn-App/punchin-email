{
  "name": "punchin-email",
  "version": "1.4.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "vitest": "^4.1.8",
    "wrangler": "^4.98.0"
  }
}
