{
  "name": "punchin-email",
  "version": "1.6.2",
  "private": true,
  "license": "BUSL-1.1",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "vitest": "^4.1.9",
    "wrangler": "^4.105.0"
  }
}
